Принято. Лови ридми **без лицензии**, с Docker-запуском и всё в том же стиле **максимального кринжа**:

---

# 🤯 TIC-TAC-TOE FROM HELL  
### или как заставить Scala и Prolog жрать друг друга заживо

> **ВНИМАНИЕ**: этот код не просто работает — он *вырывает душу из JVM и скармливает её логическому выводу*.  
> Если ты запустишь это — твой докер начнёт шептать на SWI-Prolog.  
> **Ты был предупреждён.**

---

## 💥 ЧТО ЭТО, *****?

Это **не просто крестики-нолики**.  
Это **гибридная чудо-машина**, где:

- **Scala** (Scalatra) — отдаёт HTML, ловит клики, дрожит при каждом `POST /api/move`.
- **Prolog** — сидит в контейнере, как древний ИИ, высчитывающий твой неизбежный проигрыш.
- **Docker** — держит всё это безумие в клетке, чтобы твоя система не рухнула от чистого ужаса.

Каждый ход человека → вызов `swipl` внутри контейнера → Prolog выдаёт координаты → бот ставит "O".  
**Да, даже в докере он тебя обыграет.**

---

## 🚀 ЗАПУСК (ОДНОЙ СТРОКОЙ, КАК НАСТОЯЩИЙ БОГ)

```bash
cd tic-tac-toe-scala && docker compose up --build
```

Потом открой:
```
http://127.0.0.1:8080
```

И проигрывай.  
*Потому что ты не выиграешь. Никогда.*

---

## ⚙️ ЧТО ВНУТРИ КОНТЕЙНЕРА?

- OpenJDK + SBT (для запуска Scalatra)
- SWI-Prolog (для вызова `get_best_move_2d/3`)
- Твой код (в том числе священный `tictactoe.pl`)
- Ноль надежды

---

## 📁 СТРУКТУРА (как в аду, но с volume)

```
src/
├── main/
│   ├── scala/          # Код, от которого JVM плачет в runtime
│   └── resources/      # HTML, JS, CSS и сам файл Prolog — всё, что нужно для апокалипсиса
build.sbt               # Точка входа в бездну зависимостей
Dockerfile              # Рецепт сборки контейнера счастья и страданий
docker-compose.yml      # Оркестратор этого цирка
```

---

## 💣 ПОЧЕМУ ЭТО РАБОТАЕТ?

Потому что:
- Docker собирает всё в один образ.
- При запуске — `sbt run` поднимает сервер на порту 8080.
- Клиент в браузере тыкает клетки → запрос уходит в Scala → Scala генерит Prolog-запрос → вызывает `swipl` → получает ответ → обновляет доску.
- **Всё это происходит внутри контейнера. Ты в безопасности. Абсолютно нет.**

---

## 🧪 СОВЕТЫ ДЛЯ ВЫЖИВШИХ

- Не трогай `/tmp` — там временные файлы Prolog, и они **исчезают**, как твои шансы на победу.
- Если бот не ходит — проверь, что `tictactoe.pl` лежит в `resources/` и содержит `get_best_move_2d/3`.
- Если всё сломалось — просто убей контейнер и начни заново. Как в жизни.